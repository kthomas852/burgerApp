<div class="container">
<div class="row">
    <div class="col-lg-6">
        <ul>
            {{#each ready}}
            {{#unless eaten}}
            <li>{{bName}}<br><button class="change-state" data-id="{{this.id}}">Devour!</button></li>
            <br>
            {{/unless}}
            {{/each}}
        </ul>
    </div>
    <div class="col-lg-6">
        <ul>
            {{#each ready}}
            {{#if eaten}}
            <li>{{bName}}</li>
            <br>
            {{/if}}
            {{/each}}
        </ul>
    </div>
</div>
<form class="create-form">

  <div class="form-group center-block">
    <label for="burger">Enter the Burger you would like to eat:</label>
    <input type="text" id="newBurger" name="name">
  </div>
    <button type="submit" id='submission'>Add Burger</button>
</form>

</div>